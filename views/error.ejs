<% layout('layouts/boilerplate2') %>

<div class="d-flex align-items-center justify-content-center vh-100 px-5">
    <div class="alert alert-danger px-5" role="alert">
        <h4 class="alert-heading text-center"><%= err.message %></h4>
        <!-- <% if (process.env.NODE_ENV !== "production") { %>
        <p><%= err.stack %></p>
        <% } %> -->
        <hr>
        <p class="mb-0 text-muted text-center">Please try again later</p>
    </div>
</div>